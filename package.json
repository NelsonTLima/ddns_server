{
  "name": "ddns_server",
  "version": "0.0.0",
  "description": "",
  "private": true,
  "workspaces": [
    "api",
    "app",
    "workers",
    "shared/*"
  ],
  "scripts": {
    "dev": "docker compose -f dev.docker-compose.yaml up -d --build",
    "start": "docker compose up -d --build",
    "down": "docker compose down",
    "build": "TAG=$(git describe --tags --abbrev=0) docker compose build",
    "build:latest": "TAG=latest docker compose build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
